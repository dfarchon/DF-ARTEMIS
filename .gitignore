node_modules
.env
coverage
coverage.json
typechain
typechain-types

#Hardhat files
cache
artifacts


yarn-error.log
abi
deploy-log.md
test
dest


# From https://github.com/latticexyz/mud
.DS_STORE

node_modules
# package-lock.json
# yarn.lock

.eslintcache
.parcel-cache
.docs
lerna-debug.log
.retype
.env
yarn-error.log

# We don't want projects created from templates to ignore their lockfiles, but we don't
# want to check them in here, so we'll ignore them from the root.
templates/*/pnpm-lock.yaml


